---
description: Start the Vite React dashboard dev server on 0.0.0.0
---

Run `npm run dev` to start the dashboard development server. It listens on 0.0.0.0. Confirm the port and URL once it starts.
