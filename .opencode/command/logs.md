---
description: Tail logs for a specific Docker Compose service (e.g. /logs server, /logs mysql)
---

Run `docker compose logs -f ` (replace `` with the service name provided by the user). If no service is specified, list available services from `compose.yaml` and ask which one.
