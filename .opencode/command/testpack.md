---
description: Run the ERP knowledge regression test pack (200 questions). Optional: /testpack --size N
---

Run `node scripts/run_erp_knowledge_testpack.mjs` with any options the user provides (e.g. `--size 100`, `--size 200`).

Default baseline: 200 PASS / 0 PARTIAL / 0 MISS. Report the final PASS/PARTIAL/MISS counts.
