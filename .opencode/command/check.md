---
description: Syntax-check all dashboard and server scripts with Node.js --check
---

Run `npm run check` to validate syntax of all `.mjs` and `.jsx` files in the project. Report any failures with file paths and line numbers.
